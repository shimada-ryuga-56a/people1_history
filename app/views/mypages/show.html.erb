<div class="m-5">
  <div class="mb-5">
    <h1 class="flex justify-center text-3xl"><%= title "マイページ" %></h1>
  </div>

  <div class="divider mb-3">
    <h2 class="font-bold sm:text-lg md:text-xl">
      プロフィール
    </h2>
  </div>
  <div class="flex justify-center">
    <div class="flex items-center">
      <img src="<%= current_user.image %>" class="w-50 rounded-full mr-3">
      <%= current_user.name %>
    </div>
  </div>

  <div class="divider mb-5">
    <h2 class="font-bold sm:text-lg md:text-xl">
      参加イベント
    </h2>
  </div>
  <% if @events.present? %>
    <div class="mb-5 flex items-center justify-center">
      <p>
        計<%= @events.length %>回、PEOPLE 1のライブに行きました！
      </p>
    </div>
    <% @events.each do |event| %>
      <%= render partial: 'events/event', locals: {event: event} %>
    <% end %>
  <% else %>
    <div class="mb-5 flex items-center justify-center">
      <p>
        ブックマークされたイベントがありません🤥<br>
        <%= link_to "イベント一覧", events_path, class: "link mr-1" %>から登録できます！
      </p>
    </div>
  <% end %>
</div>