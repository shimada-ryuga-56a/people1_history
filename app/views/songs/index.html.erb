<div class="m-3">
  <h1 class="text-center font-extrabold	sm:text-lg md:text-2xl mb-5">
    <%= title "楽曲一覧" %>
  </h1>

  <div class="flex justify-center">
    <div class="md:w-1/3 md:mx-3">
      <% if @songs.present? %>
        <%= render @songs %>
      <% else %>
        <div class="mb-3">楽曲情報がありません</div>
    <% end %>
    </div>
  </div>
</div>

<%= paginate @songs %>