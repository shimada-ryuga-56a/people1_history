<turbo-frame id="jacket_<%= @disc_version.id %>">
  <div class="my-3 flex">
    <% if @disc_version.jacket.attached? %>
      <%= image_tag @disc_version.jacket.variant(resize_to_limit: [1000, 1000]), class: "w-1/2 md:w-1/3" %>
    <% end %>
  </div>
</turbo-frame>