<% if infos.present? %>
  <% infos.each do |info| %>
    <div class="card bg-base-100 w-full shadow-xl mb-3">
      <div class="card-body">
        <div class="avatar flex items-center">
          <div class="w-6 rounded-full mr-3">
            <img src="<%= info.user.image %>" />
          </div>
          <%= info.user.name %>
          <p>　<%= info.created_at %></p>
        </div>
        <h2 class="card-title"><%= info.body %></h2>
      </div>
    </div>
  <% end %>
<% end %>